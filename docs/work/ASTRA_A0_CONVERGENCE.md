# Post-R4 convergence — A0 freeze and diagnosis

Date: 2026-09-20. Primary line: `integration/astra-post-r4`.
Product base: **`2baf1f6df484319e131590d37a0b026222324d03`**, accepted R4.
Reference pack: `29bb0690aa36b0e4c3a084917d120367aa61257c`.
Local worktree: `C:/Dev/qoder-atlas/.worktrees/astra-post-r4`.

## Launch and provenance

R4 is accepted: **R4 ACCEPTED — SARAH LIVE VERTICAL + PRODUCT PARITY READY**.
The integration branch starts directly at the exact accepted commit. Only the five
requested pack documents were copied; no older product code or pack merge ancestry
was imported. Their audit SHAs remain historical provenance. This record supersedes
their launch-status assumptions where final R4 differs.

The parent checkout remains on `integration/pg-test-perf` at `2534175`, with its
pre-existing untracked performance logs, `.worktrees/`, and `r4-drive1.mjs` untouched.
Other R4/refactor worktrees are not convergence lanes and will not be reset or reused.

Accepted evidence is reused, not rerun ceremonially:

- Sarah physical programme path: Reset 52/67 -> disruption 51/67 -> approved programme
  intents -> observed canonical changes -> CURRENT/PASS and RESOLVED -> 52/67.
- Atlas and Qwen LIVE at accepted boot. External transport sandbox proof is separate
  from Sarah's winning programme strategy.
- R4 broader runs: fast PG 533 pass/2 fail; CURRENT 1112 pass/4 fail; canonical PG
  595 pass/2 fail/1 skip. Deterministic failures were closed focused. The opaque
  `m9ReadModelCurrentness` suite-only failure passed alone; no broad rerun claimed.
- Final R4 added durable order-ref checkpointing, reconcile-only stale dispatch,
  duplicate-order validation and research-mode execution refusal. Preserve these.

## Current reality and decisions

WHAT WE KNOW: `main.ts` boots `composeTargetBoot`; one coordinator feeds the normal
PostgreSQL reassessment/progression services. Internal execution and sandbox offer
execution use separate executors under the same viability/authority lifecycle.
V5.6 and V7.2 renderers and a bounded Overview projection already exist.

WHAT WE DO NOT KNOW: actual post-convergence browser fidelity, Jordan's complete
progressive normal-boot path, and executable clean-reset inputs are not yet proven.

KEY ASSUMPTION: existing typed canonical state can supply the missing timing,
purpose, commitment and shared-dependency presentation. Projection changes come
before any proposal to extend domain ontology. No new workflow engine is needed.

WHAT SHOULD BE TESTED NEXT: focused graph semantics/geometry and frontend user-job
checks; then direct PG projection seams and browser comparison to accepted prototypes.
Jordan foundation must separately prove baseline -> safe -> tight -> impossible ->
case through generic ingress/evaluation before planner/UI compensation.

## Revalidated gaps and disposition

| Finding | Classification | Action and risk of deferral |
|---|---|---|
| V5.6 emits a Recovery case/workflow node; no meaningful TIMING or objective projection | Act Now | Project existing state, remove workflow content from focused graph. Deferral prevents the accepted causal explanation. |
| V5.6 missing relationship state borrows target tone; Disruption Path frames all nodes | Act Now | Preserve neutral missing-state treatment and frame supplied causal refs. Deferral invents relationship truth and weakens interaction. |
| V7.2 browser constructs relationship health; backend landmark consequence uses generic journey status | Act Now | Explicit backend relationship conditions and commitment-specific evidence. Deferral can claim a missed commitment without evidence. |
| V7.2 selects transport-only dependencies and drops ordinary graph population without programme days | Act Now | Generalize bounded source projection and preserve unassigned population. Deferral violates generality/population visibility. |
| Graph readability, compact scaling, all-clear and polling interactions | Investigate Now | Physical desktop/mobile comparison after semantic fixes; static existence does not prove fidelity. |
| Older pack says Sarah/R4 unfinished | Ignore / Accept Risk | Superseded by final R4 evidence; repeating acceptance now would waste effort. Sarah reruns on A2 candidate. |
| Missing resettable booking identity/budget; per-offer time/quote absent on strategy view | Act Now for Jordan | Reclassify R4 parks because A3/A4 require executable, reviewable replacement. Keep refusal until authoritative inputs exist. |
| R4 opaque currentness suite-only failure | Investigate Now | Carry forward; investigate if focused/new integration evidence reproduces. No blind broad reruns. |
| Budget-injection replan race, N5-N7, unrelated provider restoration | Park for Later | Preserve R4 dispositions; revisit only on objective-blocking evidence. No acceptance claim for these capabilities. |
| Overview redirect and collapsed supplier Apply control | Ignore / Accept Risk | Accepted R4 behavior retained; persistent Reset remains required. |
| Living architecture/roadmap still describes pre-R4 capability state | Act Now | Add current status without rewriting immutable milestone evidence; otherwise future routing starts from false premises. |
| Jordan D1 leaves 85 minutes against the current 90-minute hard minimum | Investigate Now | Resolve data/requirement semantics before A3: current evaluator correctly fails it. A single hard threshold does not supply a separate non-blocking risk band. Never label FAIL safe in the UI. |
| Dataset has ordered legs but no explicit CONNECTS_TO rows; normal progression not proven | Act Now for A3 | Test real source ingress through invalidation/evaluation/escalation before changing dependencies. Consecutive-leg evaluation already exists; missing explicit edges alone is not proof of an engine defect. |
| Test-only named Jordan progression constant remains in application source | Act Now for A3 | Move the illustrative path into test data; do not introduce scenario-aware runtime use. |
| Multiple simultaneous Overview blast centres | Park for Later | Current bounded projection highlights one shared change; revisit when a required acceptance world needs multiple independent centres. |
| Traveller handler uses operator shell; its own topbar lacks event context | Act Now | Use the existing separate traveller register. Deferral violates the explicit mobile product floor. |
| Overview emits hidden rows after ten without a controller; ordinary rows lack Traveller navigation | Act Now | Restore search/paging and distinct headings/interaction links. Later A1 browser check found CSS overrides hidden: all 67 appear in a 6189px page, not inaccessible rows. Deferral still loses usable roster navigation. |
| Programme is schedule-only; Decisions lacks recent decisions | Act Now | Restore roster/context/preview and durable decision-history projections in separate packages. Deferral loses legacy operator jobs. |
| Case suppresses its sole activity row; Decisions trip possessive has encoding damage | Act Now | Bounded presentation fixes with focused assertions. Deferral hides actual progress and degrades copy. |
| Traveller choice/composer has no exposed current v2 seam | Investigate Now | Inspect current request/decision commands before a bounded adaptation. No fake controls; unresolved user-job parity blocks A1 acceptance. |
| Activity history is capped at 50 with no paging | Act Now for A1 | Restore bounded cursor/page access with sanitized rows; R4 deferral does not retire the legacy history job. Deferral prevents older history review. |

Frontend findings were checked against current handlers, shell, adapters and product
renderers. Graph, frontend and Jordan A0 lanes were read-only (Terra High, Luna High,
Terra High respectively); no provider calls or test results are implied by their
inspection. Primary reclassified Activity paging from the suggested R4 park to A1
work because this mission explicitly restores the legacy user job.

## Contract boundaries for implementation

- Backend owns condition, causal evidence, breakpoint, blast radius, programme
  consequence and assessment currentness. Browser owns geometry, camera, selection,
  semantic zoom and tone-derived pulse only. Missing truth remains neutral/unknown.
- Timing/purpose are display projections, not new authoritative aggregates.
  Any additive display-kind/schema change must be reviewed by the primary before
  parallel consumers implement it. Existing Original snapshots stay immutable.
- Programme participation does not itself prove a failed commitment. Shared
  dependency selection must support meaningful non-transport state without requiring
  new hotel/transfer transaction providers.
- Provider default, selected flight and whole-trip outcome remain distinct. No
  execution from REPLAY evidence; unknown outcomes reconcile before any retry.
- Traveller is a separate mobile register; operator Reset persists. V5.6 replaces
  the causal visual, not the Case commitment/authority context or decision evidence.

## Execution decomposition and stop boundaries

1. **A1a visual corrections (Luna):** Case scene edge truth/framing/copy and focused
   geometry tests, isolated from persistence and shared contract changes.
2. **A1b graph semantics (Terra):** current Case timing/purpose/commitment projection;
   then Overview relationship/consequence/shared-dependency projection. The shared
   `readModels.ts`, `types.ts`, and `pgFactAssembler.ts` have one owner at a time.
   Freeze additive projection contracts before renderer adaptation.
3. **A1c product surfaces (Luna/Terra):** bounded legacy user-job restoration with
   an explicit backend seam where required. Separate component owners; no shared
   edits to shell/handlers while another lane owns those files.
4. Primary integrates committed/pushed lanes, proves browser behavior and accepts
   A1. A2 repeats Sarah physically on that candidate.
5. **A3 Jordan foundation and recovery (Terra):** isolate database; establish
   progressive canonical input/evaluation first, then resettable execution inputs
   and current protected flight execution. Primary owns authority ambiguities.
6. **A4/A5:** actual product Jordan, cross-scenario repeats, scoped failure evidence,
   static/broad gates, source-of-truth reconciliation and final candidate push.

Lane worktrees branch from a pushed integration SHA. No shared dirty state or test
database. Integrate producer contracts before dependent renderers; never merge a
lane-local contract variant. Primary owns `test/suites.json` registrations, shared
seam reconciliation, checkpoint ledger and final verification.

Only use Sol if a named consequential/authority/reconciliation uncertainty remains
after focused execution evidence. No generic review committee.

## A1 presentation decision — Overview density

- **WHAT WE KNOW:** the real bounded read model supplies twelve shared dependencies,
  nineteen programme cards and the full population. Drawing every dependency at the
  compact viewport forced roughly half-size text. The accepted visual uses a small
  number of shared dependency cards. Backend relationship kinds and health remain
  authoritative; replacement changes now include applied canonical provenance.
- **WHAT WE DO NOT KNOW:** final mobile and repeated-poll interaction acceptance is
  still pending; a readable expanded screenshot is evidence, not complete A1 acceptance.
- **KEY ASSUMPTION:** four shared dependency cards, with the active incident retained
  first and remaining backend-ranked groups in an adjacent disclosure, is a faithful
  bounded presentation. Every programme and population/cohort card stays available.
  Full selected-card text is exposed in an accessible inspector; expansion and Whole
  event framing provide the wider context. No business assessment is inferred here.
- **WHAT SHOULD BE TESTED NEXT:** selection/disclosure/camera persistence, Whole event
  after expansion, narrow viewport, and baseline-to-disruption summary refresh.
- **Act Now:** complete those checks before A1. Deferral could hide context or impair
  navigation even though all backend facts exist.

## Verification

A0 is docs-only: exact base/ancestry, reference provenance, diff/status and whitespace
checks. No product tests or providers run for A0. New broad counters start at zero.
Focused -> adjacent -> isolated PG seam -> browser/provider -> coherent fast PG ->
major CURRENT -> meaningful canonical PG. Stop a failed broad gate and reproduce
its smallest failing signal before deciding on another run.
