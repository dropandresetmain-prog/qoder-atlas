# A1 desktop product convergence — evidence in progress

Accepted product base: `2baf1f6df484319e131590d37a0b026222324d03`.
Integration branch: `integration/astra-post-r4`. A1 is not accepted until its final
checkpoint is recorded in ACTIVE_TASK. No merge to main.

## Scope decision

Founder redirected A1 to strong desktop operator/product surfaces, faithful V5.6
and V7.2, credible Programme/Decisions/Activity/Traveller views, persistent Reset,
and an operable Sarah path. Strict legacy parity may be deferred with reasons.
After A1, proceed immediately to A2 Sarah physical acceptance and A3 Jordan runtime.
Mobile is a separate post-hackathon milestone. Luna/Terra own bounded work; Sol
is reserved for a named unresolved critical risk.

Latest [hero-depth contract](ASTRA_HERO_DEPTH_SCOPE.md) supersedes blanket ancillary
and programme-operation deferrals below: A2 is Sarah LIVE/video-ready; A3 is Jordan
multi-domain composition; A4 is real sandbox/video-ready Jordan; A5 repeats both LIVE.

## Cumulative focused evidence

- Original accepted R4 evidence remains authoritative; no ceremonial reacceptance.
- Case semantic enrichment and real PG graph seam: 7/7; renderer and language
  tests cover arrival/commitment hierarchy, supplier observation and neutral unknowns.
- Overview semantic PG seam: full 69-journey test population retained, generic
  shared resource relationships, five-person replacement-service footprint.
- Programme/Decisions/Activity PG seam: 3/3, including 20-row keyset history and
  distinct decision/revocation actors. Programme projection retains all attendees.
- Request persistence/admission groundwork has focused PG evidence, but is unwired
  in normal product submission; healthy-trip request planning is not integrated.
- Programme import HTTP preview/import/retry: 4/4 before the bounded interruption
  investigation. Validation preview writes nothing and shares the import schema.
- Programme time-swap staging: real PG preview/staging seam 2/2; deterministic
  strategy identity includes actual assessment/world manifest, not only schedule revision.
- Overview overlap correction: 6/6 focused browser tests. Primary also reproduced
  the blocked click before the correction and proved normal pointer selection after it.
- Traveller canonical timezone presentation: 14/14 focused surface tests, including
  different endpoint zones and UTC fallback. Intake/read-only interpretation: 9/9.
- TypeScript and test-boundary gate passed on integrated work before final preview
  correction; repeat only the final integration checks needed by that correction.

## Physical evidence already obtained

Normal `main.ts` boot uses PostgreSQL database `astra_product`, workspace
`9ef64348-61b2-4e18-a291-152502a89a02`, port 4120. These A1 checks use REPLAY Atlas,
without Qwen credentials or external execution. They do not claim LIVE acceptance.

- Persistent Reset completed and returned a clean 52/67 baseline with nobody needing attention.
- Applying the disclosed supplier input changed Overview to 51/67, one person needing
  attention/decision, and four cleared/one remaining in the shared-service footprint.
  The graph camera survived the polling update.
- Current Sarah Case after reset: `2c4e203c-a97a-52d0-801b-152ca81652dc`.
  It awaits programme approval; no recovery execution was performed for A1.
- Case graph shows source change -> confirmed supplier replacement -> failed arrival
  timing at 10:30 GMT+8 -> programme commitment at 11:30 GMT+8. Selection exposes
  full booking detail; selection/camera survive polling. Original remains selectable.
- Overview disrupted-service click, expanded framing, Whole event, attention focus,
  selected detail and extra shared dependencies are operable. Disclosure survives polling.
- Programme navigation, searchable roster, real time-swap preview and intake route work.
  Physical preview exposed first-attendee-only, baseline-verdict and timezone issues;
  all closed by `cc40c004` and physical recheck. Staging then exposed all-participant
  must-pass/arbitrary-basis errors; `58d04aa2` closes these with real Case failure basis
  and RC-6 non-worsening peers, focused PG 3/3. Desktop Continue to approval now succeeds,
  says the programme has not changed, and opens the Case with the exact staged alternative.
  Current canonical session times remain unchanged; no recovery executed.
- Decisions shows one real pending organiser decision and honest empty recent history.
- Activity shows 20 events and Older activity loads an earlier page.
- Intake form validates synthetic session/attendance data, then enables Import with
  “Nothing has been imported.” No extra event was added to the demo world.

Screenshots/snapshots stay under ignored `output/playwright/`; they are local visual
evidence, not committed provider recordings. Final desktop screenshot/preview and
checkpoint-gate results remain to be added.

Final desktop closure on integrated `e63e00ff`:

- Stay window now uses canonical Singapore zone: 29 Sep15:00 ->3Oct11:00GMT+8.
  Focused closure `f718f20`: 76unit/8PG, TypeScript green.
- Clicking the staged alternative actually approved/applied it. The old selection-like
  label was misleading; `780a048` changes it to “Approve and apply this option” with
  unchanged approval gates, focused B1 20/20. This is a real A1 REPLAY execution,
  not merely a staged proposal and not A2 LIVE acceptance.
- Observed Case recovered/resolved after programme actions; Overview52/67, nobody
  needing attention; Decisions zero waiting and two recorded programme approvals.
- Local visual artifacts: `a1-final-case-recovered.png`, `a1-final-overview-recovered.png`.
- Latest boot is now LIVE Atlas research/Qwen plus protected Atlas sandbox execution;
  the completed recovery above still belongs to the earlier REPLAY run. A2 resets fresh.

CURRENT_TARGET attempt 1 on `154e93f`: 1161 pass / 4 fail in 50 seconds. Three
failures expected old timezone-free Case copy; the fourth found two new canonical
PG tests missing from the fast subset. Fast membership corrected and its focused
contract passes 3/3. Case expectation closure `e3db176` passes 20/20; no CURRENT
rerun as a debugging loop. Final integrated TypeScript and boundary gate pass
(260 classified files). Full canonical PG remains unrun for A1.

postgres:fast attempt 1 on isolated `astra_a1_checkpoint_20260920` was stopped
after five failures in `m2SubtypeIntegrity.pgtest.ts`; focused diagnosis is active.
Do not claim a completed broad pass or count unrun files as verified.

Attempt1 registry failures closed15/15 (`a92fbff`). Attempt2 was stopped after one
`m10DemoSeed` assertion failure. Diagnosis found an old global-ID inequality assumption
in the demo-seeder test. The schema identifies roots by `(workspace_id,id)`; changing
the import namespace now would break existing retries. Primary rejected unintegrated
`7b8236ed` and requested stronger workspace-isolation/idempotency proof with no importer
runtime change. Unfinished checkpoint files remain to be run after focused closure.

## Explicit deferrals and risks

- **Park for Later — healthy-trip requested-state planning.** The current failure-triggered
  coordinator cannot safely promise desired-state fulfillment merely from a CURRENT PASS.
  Canonical request identity, constraints, progression, authority and observed satisfaction
  require one deliberate extension. Do not fake FAIL or create a second planner.
  Lane `be69d056` is not integrated. Its review identified caller-authored basis,
  request-blind resolution, proposer-claimed constraints, stale existing-plan authority,
  original-date research and non-durable unsupported reasons. Partial untested fixes
  remain isolated on `codex/astra-a1-request-gates` for later inspection.
- **Act Now for heroes — typed programme change.** Current bilateral time swap is
  narrower than historical programme capability. Relocation/cancellation remains required
  where a hero strategy needs it, with typed effects/executors and a distinction between
  announced source change and proposed recovery. Unrelated operations stay parked.
- **Ignore / Accept Risk — legacy message/export footer.** Those controls were inert;
  updated-sheet navigation was not fully wired. No functional messaging/export parity claimed.
- **Park for Later — importer atomicity.** Focused PG interruption/retry proof `646ed3e`
  passes 5/5. The committed prefix remains valid and UNKNOWN, retry converges to exact counts
  without duplicates, another workspace stays unchanged, no supplier/authority effects occur.
  Incomplete inventory can remain visible; revisit only if it destabilizes hero/reset.
- **Park for Later — mobile UI and non-critical parity.** Separate post-hackathon work.
- **Act Now for A3 — Jordan data/runtime foundation.** Recorded D1 gives 85 minutes
  against a hard 90-minute connection minimum; final-snapshot replay cannot prove stages.
  Protected identity/budget reset inputs and contradictory nationality need reconciliation.
  Keep these distinct from A1 acceptance. Jordan multi-domain depth, relevant LIVE research
  and supported selected sandbox actions are required under the latest hero contract.

## A1 checkpoint acceptance — 2026-09-20

A1 desktop scope is accepted on source/test head `474f95d580c59638e5d5cfbbcdfd989d50c4a0b8`.
The checkpoint commit containing this section records acceptance; verify the pushed branch
SHA in Git. Explicit deferrals above remain visible and do not claim full legacy parity.

- Demo-seed test closure `581cf82`: 1/1 focused PG, TypeScript, workspace-scoped identity,
  exact retry counts and independent mutation. No importer production change.
- Final unfinished 33-file PG segment: 131 pass / 0 fail / 0 skip, exit0, 457.7 seconds.
  Local log `output/playwright/a1-postgres-checkpoint-remainder.log` and `.exit`.
- Counters: postgres:fast 3 (third is suffix only), CURRENT_TARGET 1, full canonical PG0.
  This is cumulative passing segments plus focused failure closure, not a claimed single
  all-green broad rerun. No broad debugging loop or weakened runtime invariants.
- Final desktop recovery, graph interactions and canonical timezone evidence above are
  accepted for A1. The earlier pending/no-execution descriptions are chronological states;
  the final A1 state is REPLAY RESOLVED and 52/67 with nobody needing attention.
- Existing programme nodes without an independent assessment retain UNKNOWN. Do not
  manufacture PASS from a parent journey; retain R4 classification unless hero readability
  provides evidence that a stronger authoritative projection is needed.

Next: fresh Reset under the already composed LIVE boot, followed by Sarah live research,
recommendation, authority, execution, observation and whole-trip reassessment. A2 requires
actual live provider/model evidence, not only boot configuration.
