# Capabilities and limitations

**Current convergence status (2026-09-21):** R4, A1 and A2 are accepted. A3 remains a historical conditional presentation pass. A4 is **PHYSICALLY ACCEPTED** on `546adf210db8ead343ecdac22b410515665c176a`. Founder A5.1 E2E exposed two active convergence lanes: one-time visual-first Overview/Case redesign plus bounded backend/read-model truth closure. A5.2 physical Sarah + Jordan proof and A5.3 gates/freeze follow only after both lanes integrate.

This is the technical truth sheet for the current PostgreSQL target runtime. `IMPLEMENTED` means an executable path exists; `COMPOSED` means normal boot can reach it; `FOCUSED-PROVEN` means the relevant seam passed; `PHYSICALLY ACCEPTED` is reserved for closed end-to-end evidence.

## Current runtime truth

- PostgreSQL + PostGIS is the sole normal runtime. SQLite is historical/offline migration input only.
- One generalized RecoveryPlanningCoordinator owns product planning. AI interprets/researches/proposes; deterministic code owns hard viability, authority, state mutation and consequential side-effect gates.
- A2 Sarah LIVE is accepted: Qwen + Atlas research, generalized programme-side recovery, observation, reassessment and RESOLVED state.
- A3 Jordan recommendation/UI is a **CONDITIONAL PASS**: the complete recommendation is usable, while a bounded graph/progression/copy truth pass remains.
- A4 Jordan physical execution is **ACCEPTED**: replacement flight TICKETED, Narita stay CONFIRMED, destination replacement stay CONFIRMED, displaced stay CANCELLED, all successes canonically applied, final whole-trip assessment CURRENT/PASS, Case RESOLVED/RECOVERED.
- Selected-plan continuation supports compound canonical footprints from one action using exact durable receipts rather than fixed revision increments.
- Protected Nuitée stay book/cancel/reconcile is implemented, composed and physically proven in the selected A4 path.
- Destination replacement research may use a bounded alternate property when the preferred property lacks a valid quote.
- The overnight-required Jordan stage emerges from boardability at the scenario clock; no force-overnight application branch exists.
- Unknown external outcomes never permit blind redispatch. Provider success alone never resolves a trip.

## Current capability matrix

| Capability | Current truth | Forward boundary |
|---|---|---|
| Persistence / canonical ownership | **IMPLEMENTED / ACCEPTED** | Preserve PostgreSQL ownership. |
| ChangeSignal / mutation / invalidation | **IMPLEMENTED / ACCEPTED** | Preserve. |
| Whole-trip deterministic assessment / RC-6 | **IMPLEMENTED / ACCEPTED** | Preserve deterministic veto. |
| RecoveryCase lifecycle / progression | **IMPLEMENTED / ACCEPTED** | Preserve one lifecycle owner. |
| Recovery-domain planning | **IMPLEMENTED / ACCEPTED CORE** | No new planner architecture in A5. |
| Internal programme execution | **A2 PHYSICALLY ACCEPTED** | Preserve Sarah path. |
| Desktop V5.6 / V7.2 surfaces | **IMPLEMENTED; founder-QC requires redesign around accepted graphs** | Preserve V5.6/V7.2 semantics; redesign surrounding Overview/Case composition visual-first before production integration. |
| Selected-plan continuation | **IMPLEMENTED / PHYSICALLY PROVEN IN A4** | Do not broaden into generic workflow. |
| Protected Atlas selected execution | **IMPLEMENTED / PHYSICALLY PROVEN IN A4** | Preserve no-blind-retry semantics. |
| Protected Nuitée book/cancel/reconcile | **IMPLEMENTED / PHYSICALLY PROVEN IN A4** | Preserve exact-binding and canonical-application gates. |
| Multi-domain Jordan planning | **RECORD/LIVE-PROVEN** | A5 repeatability/setup only. |
| Traveller/mobile/request composer | **PARTIAL / PARKED** | Post-hackathon. |

## A4 provider acceptance evidence

- Atlas: order `TESTA20260921012546645`, locator `S89153`, **TICKETED**.
- Narita stay: Nuitée `blxskTP7r`, USD 35.29, **CONFIRMED**.
- Destination replacement: Nuitée `b347Hn3BU`, USD 109.47, **CONFIRMED**, property `lp19483c`.
- Displaced destination booking: `c4NsnfT_N`, **CANCELLED**.
- Final assessment: `4c4c5698…`, **CURRENT / PASS**.
- Final Case: **RESOLVED / RECOVERED**.

See [A4 physical acceptance](work/A4_PHYSICAL_SANDBOX_ACCEPTANCE.md) for the execution chain and fixes.

## Current A5 gaps

### Act Now

- visual-first redesign of Overview and Case around the accepted V7.2/V5.6 graphs: image mockups → founder QC → HTML prototypes → founder QC → production integration;
- preserve Sarah + Jordan simultaneously in one product world; a reset is not a product scenario switch;
- fix authoritative Jordan progression so D1/D2/D3 presents GREEN → AMBER → RED rather than collapsing TIGHT and IMPOSSIBLE into one top-level failure state;
- fix D3 focused Case causality and any duplicate connection projection;
- gate D2 monitoring/risk separately from premature actionable replacement recovery;
- reproduce the exact Jordan approval blocker and fix either product logic or founder-QC provisioning without bypassing authority/budget/execution safety;
- inspect suspicious Sarah FAILED node/edge states for incorrect Case-wide inheritance;
- integrate both lanes, then repeat founder E2E before A5.2;
- run final physical Sarah + Jordan proof on one exact candidate, then full gates once.

### A5 baseline requirements

- provision sandbox budget envelopes before disruption/planning;
- use a fresh active displaced destination booking for destructive Jordan repeats;
- keep the scenario clock authoritative during synthetic progression/reassessment;
- union same-credential intended-visit scopes on stay attachment;
- account compound selected-action scope bumps from exact receipts;
- preserve provider state and reconcile uncertainty rather than restarting.

### Park for Later

- healthy-trip requested-state planning / traveller composer;
- mobile acceptance;
- generic programme, hotel, itinerary, visit or credential administration;
- broad immigration/legal crawling;
- unrelated providers, transfer transactions and insurance claims;
- extra scenarios and multi-centre V7.2 graph breadth; simultaneous open Sarah/Jordan Cases are now an A5 product requirement, not deferred;
- importer atomicity redesign and non-critical legacy parity;
- unrelated infrastructure/refactors.

### Ignore / Accept Risk

- inert legacy message/export controls and accepted R4 redirects unless a hero-visible blocker appears.
