# M8 evidence — Checkpoint 0 (pre-M8 conditions)

Status: **Checkpoint 0 CLOSED** on branch `milestone-m8-authority-execution`.

| Item | Value |
|---|---|
| Base | `a0843db73c5e4bd7fb5c1874391d7361b789aed1` (`integration/m2-m6-domain-evaluation`) |
| Package | M8 Checkpoint 0 — I-10 / G-B / currentAssessment gate / reassessment complete retry |
| Does not claim | C3; full M8 candidate; M9 |

## Closures

### 1. I-10 — exact FX / money arithmetic

- Authoritative seam: `src/domain/v2/shared/money.ts`
  - `currencyExponent` (0 / 2 / 3)
  - `multiplyExactDecimal` (HALF_AWAY_FROM_ZERO)
  - `convertExactMoney` (sourced FX; missing/expired/mis-oriented → not ok)
  - `addExactMoney` / `compareExactMoney` use currency exponent when omitted
- `src/resolution/evaluation/evaluators/funding.ts` uses the shared seam only (no local BigInt FX).

### 2. G-B — traveller payer currency UNKNOWN

- `src/resolution/authority/payerCurrencyGate.ts` denies authority when funding
  reports `payer_home_currency_unknown`. Currency is never invented.

### 3. Decision-time `currentAssessmentView`

- `src/resolution/authority/assessmentGate.ts` + `decisionGates.ts`
- STALE / PENDING_REASSESSMENT / UNAVAILABLE / NONE → not authorised
- PG proof: CURRENT → gate open → aggregate advance → PENDING → dispatch refused

### 4. `PgReassessmentWorker.complete()` retry / requeue

- Bounded in-process retries on `40001` / `40P01` / `23505`
- Then durable requeue to PENDING (clear claim token; **fencing unchanged**)
- Failed complete transactions roll back → no duplicate successful assessment
- PG proofs for forced serialization and unique-violation paths

## Checks run

| Check | Result |
|---|---|
| Unit Checkpoint 0 money + authority gates + L3 funding I-10 + contracts money | pass |
| `postgres-integration/m8Checkpoint0.pgtest.ts` (fresh DB) | 3/3 pass |
| `npm run typecheck` | clean |

## Next

Proceed to main M8 authority / budget / durable execution / observation / reconciliation implementation. Do not start M9. Do not claim C3.
