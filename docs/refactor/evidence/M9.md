# M9 — Application composition and product integration

Status: **CHECKPOINT 1 COMPLETE — Checkpoint 2 waiting on Sarah/product handoff**

## Identity

| Item | Value |
|---|---|
| Accepted C3 base | `integration/m7-m8-c3` @ `f8103379ed2426d442d342895e9e1d1573f875da` |
| M9 branch / worktree | `milestone-m9-product-integration` / `C:/Dev/qoder-atlas-m9` |
| Working ledger | `docs/work/M9_ACTIVE_TASK.md` |

## Checkpoint 1 — Core product integration

### Target runtime topology

- Live legacy SQLite path remains at `src/main.ts` → `src/app/compose.ts` (M10/reference only).
- Target composition: `src/app/target/composeTargetApplication.ts` → `composeTargetRuntime` (PostgreSQL pool, migrations, UoW, reassessment worker).
- `sqliteAuthoritativeFallback: false` on the target application object.
- Application commands: `src/app/target/applicationCommands.ts` (open case, persist strategy/plan, grants, resolution, demo ingress accept, disposition denial).

### RecoveryStrategy persistence

- Command: `persistRecoveryStrategy` in `src/persistence/postgres/commands/m7StrategyCommands.ts`.
- Persists identity/version, case, non-empty base_manifest, scenario_change, affected subjects (`strategy_changes`), candidate summaries, viability, assumptions/unknowns, authority scopes, rejection/provenance fields.
- Empty base_manifest refused at the command boundary.

### R-10 grant issuance

- Facade: `issueRequiredAuthorityGrant` / `provisionOrganiserAuthority` in `src/app/target/grantIssuance.ts`.
- Wraps real `issueAuthorityGrant`; caller cannot propose a weaker scope than deterministic `requiredScopes`.
- No Organisation→Journey inheritance (exact TypedRef containment only).

### IN-1 re-plan identity

- Frozen rules: `src/app/target/replanIdentity.ts` + migration `0120_m9_replan_identity.sql`.
- Effect-scoped `logicalOperationKey` retained.
- Intent uniqueness scoped per `(workspace, action_plan, namespace, logical_key)`.
- Execution-layer known-success / live-attempt guards still prevent duplicate irreversible dispatch.
- Confirmed failure + new strategy may persist a new plan with the same logical key.

### Objective disposition boundary

- `src/app/target/objectiveDispositionBoundary.ts`
- `M9_OBJECTIVE_DISPOSITION_API_EXPOSED = false`
- WAIVED / CLOSED_WITH_LOSS require authorised ActionIntent; no direct application mutation.

### RecoveryCase resolution gate

- Evaluate: `evaluateRecoveryCaseResolution` (`src/app/target/recoveryCaseResolution.ts`)
- Commit: `resolveRecoveryCase` (`src/persistence/postgres/commands/m9CaseResolutionCommands.ts`)
- Denies: missing/not-current assessments, FAIL, UNKNOWN, unreconciled execution, proposed-only strategy without observed success, missing required people.
- Does **not** resolve on API/provider/approval/planner viability alone.

### Read models / LDG / change-awareness

- Contracts: `src/contracts/v2/product/readModels.ts`
- Projectors under `src/app/target/readmodels/`
- Booking/service state separate from whole-trip viability.
- LDG is presentation-safe (no raw table names).
- Change-awareness carries projection revision + previous/current semantic state (no UI timers).

### Demo ingress + second scenario

- `acceptProviderShapedDemoEvent` — disclosed simulated provider-shaped event → normalise/signal path (no direct case mutation).
- Second scenario: **Jordan S2** (`jordanS2ScenarioFoundation`) — progressive individual disruption with coordinated multi-domain recovery. Supersedes the earlier traveller-initiated placeholder. Same runtime/read models/LDG; no second hero polish for M9.

### Focused verification (Checkpoint 1)

| Check | Result |
|---|---|
| `npx tsc -p tsconfig.json --noEmit` | pass |
| `node --test test/m9-checkpoint1-unit.test.ts test/m9-product-readmodels.test.ts` | 12/12 pass |
| `node --test postgres-integration/m9Checkpoint1.pgtest.ts` | 4/4 pass |
| C3 IN-1 Q4 updated for 0120 | pass |

Full `npm run test:postgres` deferred to final M9 candidate (per M9 testing strategy).

## Jordan S2 addendum (post-CK1 product contract)

**Verdict: Jordan S2 compatible — no CK1 reopening required.**

Progressive connection states map onto existing generic enums:

| Progression | ProductOperationalStatus | RemainderViability | AssessmentTone | LdgSemanticState |
|---|---|---|---|---|
| HEALTHY / SAFE | READY | VIABLE | PASS | HEALTHY |
| CONNECTION_AT_RISK | AT_RISK | AT_RISK | UNKNOWN* | AFFECTED |
| CONNECTION_IMPOSSIBLE | DISRUPTED | NOT_VIABLE | FAIL | FAILED |

Partial recovery is already expressible via separate `bookingServiceState` vs `tripViability` plus the CK1 resolution gate (provider/action success ≠ case resolved).

### Additive generic fields for Checkpoint 2 start (not implemented yet)

Recorded in `JORDAN_S2_CK2_ADDITIVE_READMODEL_FIELDS`:

- optional connection progression presentation aid
- `recoveryActions[]` (per-intent domain/cost/authority/execution/outcome/order)
- aggregate recovery cost when deterministic
- remaining recovery work / explicit partial-recovery summaries

Do **not** invent Jordan flight/hotel/timing fixtures before Atlas + Nuitée evidence.
Do **not** treat Jordan evidence as unblocking Sarah Checkpoint 2.

## OpenRouter integration (early into M9)

| Item | Value |
|---|---|
| Source lane | `feature/openrouter-provider-neutral` @ `2eeff2a2d7724b29f976857523466e7520041c01` |
| Base ancestry | Direct child of accepted C3 `f810337` (verified `merge-base --is-ancestor`) |
| Merge into M9 | Clean ort merge; touches `compose.ts` / `config.ts` / intelligence client / env docs |

### Flaky-test verification (not an OpenRouter regression)

| Test | On C3 without OpenRouter (`integration/m7-m8-c3` @ `f810337`) | On M9 after OpenRouter merge |
|---|---|---|
| `R1: the runtime loop is deterministic…` (`test/integration.r1.test.ts`) | **Fails** — wall-clock `observedAt` differs across identical runs (5/5 attempts failed) | Same failure mode (legacy SQLite runtime) |
| `rev2-r5: COMPOSED runtime planner is deterministic…` | Pass | Pass |
| `test/intelligence-provider-selection.test.ts` | n/a | 12/12 pass |

Already triaged historically as A-10 / Park for Later (legacy wall-clock nondeterminism). **Do not treat as an OpenRouter regression.**

## Checkpoint 2 — blocked

Waiting for the separate WiT/product-design workstream handoff (**Sarah**):

- final Sarah baseline flight
- disruption/reprotection facts
- operational timing requirement
- final programme schedule
- second speaker / paired recovery
- selected Fable visual direction

**Do not invent those facts in this lane.**

Jordan Atlas/Nuitée evidence remains a separate pending input for second-scenario acceptance.

## Issue triage

| ID | Class | Notes |
|---|---|---|
| ORG-INHERIT | Park for Later | Exact grant match only; architecture decision required if org→journey inheritance is needed |
| ISSUER-POL | Investigate Now (bounded) | Grant issuer policy still not table-enforced (0019 gap); demo bootstrap uses real commands |
| CK2-SARAH-HANDOFF | Park for Later | Product/Sarah facts not ready — blocks polished CK2 |
| JORDAN-EVIDENCE | Park for Later | Atlas + Nuitée pending |
| JORDAN-CK2-ADDITIVE-RM | Act Now at CK2 start | Multi-action/partial-recovery additive fields |
| SQLITE-LEGACY | Accept Risk (intentional) | Legacy SQLite app remains separately runnable until M10/M11 |

## Architecture gaps

None newly hardcoding around ontology. Org→Journey inheritance, if required by product, remains an explicit stop/decision point. No Jordan-only status field required.
